// import { Button, Input } from '@josegts/rollup-react-library-starter';

import { Button, Input } from '@mui/material';

export const Navbar = () => {
  return (
    <div>
      <Button>Holi</Button>
      <Input />
    </div>
  );
};
